package poly.ex4;

public abstract class AbstractAnimal {
	public abstract void sound();

	public abstract void move();

	//다음과 같이 추상 메서드만 존재하는 클래스를 순수 추상 클래스라고 칭하는데
	//다음과 같은 특징이 있다.
	//1.인스턴스를 생성할 수 없다 -> 부모 클래스의 메서드는 실질적으로 하는 것이 없고 오버라이팅을 위한 껍데기 역할을 하기 때문
	//2.상속시 자식은 모든 메서드를 오버라이딩해야한다.
	//3.주로 다형성을 위한 껍데기 용도로 사용한다. --> 순수 추상 클래스는 마치 어떤 규격을 지켜서 구현해야 하는 것처럼 느껴진다.
	// 마치 usb, c타입 과 같은 규격에 맞춰 제품을 만드는 것과 비슷한 느낌이다.
	// 이런한 순수 추상클래스와 똑같은 역할을 하는 것이 "인터페이스" 이다.
}
